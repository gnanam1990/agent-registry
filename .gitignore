# Foundry
out/
cache/
# Local deploy artifacts — the canonical record is deployments/arc-testnet.json.
broadcast/
# `forge doc` output only — hand-written docs/ (DEPLOY.md, etc.) are committed.
docs/book/

# node / dashboard deps
node_modules/

# secrets — NEVER commit (testnet keys count too — treat every key as real)
.env
.env.*
!.env.example
*.key
*.pem
keystore/
secrets/

# os / editor
.DS_Store
.idea/
.vscode/
*.log
